load("@bazel_tools//tools/build_defs/pkg:pkg.bzl", "pkg_tar")

pkg_tar(
    name = "PatternCollections",
    extension = "tar.gz",
    deps = [
        "//AbstractFactory:abstract-factory-tar",
        "//Adapter:adapter-tar",
        "//Bridge:bridge-tar",
    ],
)
